/**
 * 表示一个行列位置。
 */
export interface Location {
    /**
     * 当前位置的行号(从 0 开始)。
     */
    line: number;
    /**
     * 当前位置的列号(从 0 开始)。
     */
    column: number;
}
/**
 * 计算指定索引对应的行列号。
 * @param value 要处理的字符串。
 * @param index 要计算的索引。
 * @param cache 如果提供一个缓存数组则存放一个索引数据以加速检索。
 * @return 返回对应的行列号。如果索引错误则返回 0,0 位置。
 */
export declare function indexToLocation(value: string, index: number, cache?: number[] & {
    cursor?: number;
}): Location;
/**
 * 计算指定行列号对应的索引。
 * @param value 要处理的字符串。
 * @param loc 要计算的行列号。
 * @param cache 如果提供一个缓存数组则存放一个索引数据以加速检索。
 * @return 返回对应的索引。如果行列号错误则返回 0。
 */
export declare function locationToIndex(value: string, location: Location, cache?: number[] & {
    cursor?: number;
}): number;
