"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const asyncQueue_1 = require("../utility/asyncQueue");
/**
 * 获取全局的异步队列。
 */
exports.asyncQueue = new asyncQueue_1.AsyncQueue();
/**
 * 等待当前任务全部完成后执行指定的任务。
 * @param callback 要执行的任务函数。
 */
function then(callback) {
    exports.asyncQueue.enqueue(callback);
}
exports.then = then;
