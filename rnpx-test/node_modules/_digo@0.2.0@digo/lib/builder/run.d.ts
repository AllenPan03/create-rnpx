import { AsyncCallback } from "../utility/asyncQueue";
/**
 * 是否允许直接载入全局安装的模块。
 */
export declare var requireGlobal: boolean;
/**
 * 所有支持的配置文件扩展名。
 */
export declare const extensions: {
    [ext: string]: string[];
};
/**
 * 载入配置文件。
 * @param path 要载入的配置文件路径。
 * @param updateCwd 是否更新当前工作目录。
 * @return 返回配置文件定义的所有任务。如果载入错误则返回 undefined。
 */
export declare function loadDigoFile(path?: string, updateCwd?: boolean): {
    [key: string]: Function;
};
/**
 * 获取本次生成操作的开始时间。
 */
export declare var startTime: [number, number];
/**
 * 是否在生成完成后报告结果。
 */
export declare var report: boolean;
/**
 * 执行一个任务。
 * @param task 要执行的任务。
 * @param taskName 任务名。
 */
export declare function run(task: AsyncCallback, taskName?: string): void;
