/// <reference types="node" />
import * as childProcess from "child_process";
/**
 * 同步执行一个命令。
 * @param command 要执行的命令。命令行参数以空格隔开。
 * @param options 执行相关参数。
 * @return 返回程序的输出内容。
 */
export declare function exec(command: string, options?: ExecOptions): childProcess.SpawnSyncReturns<string>;
/**
 * 异步执行一个命令。
 * @param command 要执行的命令。命令行参数以空格隔开。
 * @param callback 异步执行完成的回调函数。
 * @return 返回启动的子进程。
 */
export declare function exec(command: string, callback: (status: null | number) => void): childProcess.ChildProcess;
/**
 * 异步执行一个命令。
 * @param command 要执行的命令。命令行参数以空格隔开。
 * @param options 执行相关参数。
 * @param callback 异步执行完成的回调函数。
 * @return 返回启动的子进程。
 */
export declare function exec(command: string, options: ExecOptions, callback: (status: null | number) => void): childProcess.ChildProcess;
/**
 * 表示执行的选项。
 */
export interface ExecOptions extends childProcess.SpawnSyncOptions {
    /**
     * 是否向控制台输出内容。
     */
    slient?: boolean;
    /**
     * 命令行参数。
     */
    args?: string[];
}
