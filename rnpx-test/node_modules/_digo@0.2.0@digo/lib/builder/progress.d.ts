/**
 * 是否在控制台显示进度。
 */
export declare var progress: boolean;
/**
 * 获取所有任务数。
 */
export declare var taskCount: number;
/**
 * 获取所有已完成的任务数。
 */
export declare var doneTaskCount: number;
/**
 * 记录将开始执行指定的任务。
 * @param task 任务内容。
 * @param args 格式化参数。*task* 中 `{x}` 会被替换为 `args.x` 的值。
 * @return 返回任务序号。
 */
export declare function begin(task: string, args?: Object): string;
/**
 * 记录已执行指定的任务。
 * @param task 要结束的任务序号。
 */
export declare function end(task: string): void;
